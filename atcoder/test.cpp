#include <bits/stdc++.h>
using namespace std;

template<typename x> x add(x a, x b)
{
	return a+b;
}

int main()
{
	cin.tie(0);
	ios::sync_with_stdio(false);

	int ans = add(4, 3);

	float dans = add(4.0, 3.0);

	cout << ans << endl;
	cout << dans << endl;
}
