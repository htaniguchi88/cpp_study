#include <bits/stdc++.h>
using namespace std;

int main(){
  cin.tie(0);
  ios::sync_with_stdio(false);

  // int N, A, B, C, D;

  int N;

  cin >> N;
  // cin >> A >> B >> C >> D;

  string sqr(N, '#');

  getline(cin, sqr);
  cout << sqr << "\n";
  cout << sqr[0];

  // A文字目，B文字目をA, Bで置き換える

  // 1ますもしくは2ますしか右に行けない!!
  // Bは一番近くの.に移動する
  // Aは一番近くの.に移動する


}
