#include <iostream>
using namespace std;

const int MAX_NAME = 16;

struct Student {
    char name[MAX_NAME + 1];
    int scoreJapanese;
    int scoreMath;
    int scoreEnglish;
};

void Show(const Stundent& student) //Stundentへの参照を引数にとる関数show
{
    cout << "名前"
}
